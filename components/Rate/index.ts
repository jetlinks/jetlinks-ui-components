import { withInstall } from '../util/type';
import Rate from 'ant-design-vue/lib/rate/index';

export type { RateProps } from 'ant-design-vue/lib/rate/index';

export const JRate = withInstall(Rate, 'JRate');

export default JRate;
